{
  "name": "chat2shop-firebase-rules",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "firebase --config ../firebase.json emulators:exec --project chat2shop-rules-test --only firestore,storage \"npm run test:rules\"",
    "test:rules": "tsx tests/rules.test.ts"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.13.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.2"
  }
}
